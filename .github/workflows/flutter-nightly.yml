name: Patch config.rs
        run: |
          sed -i 's/pub const RENDEZVOUS_SERVERS: &[&str] = &\[""\];/pub const RENDEZVOUS_SERVERS: &[&str] = &["10.96.16.53"];/' libs/hbb_common/src/config.rs



name: Flutter Nightly Build

on:
  schedule:
    # schedule build every night
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
